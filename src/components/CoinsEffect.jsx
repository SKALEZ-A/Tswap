// This is a helper file to be included in Dex.jsx
// Add this useEffect after the existing useEffects in the Dex component:

/*
// Update filteredCoins when coins prop changes
useEffect(() => {
  if (coins) {
    setFilteredCoins(coins);
  }
}, [coins]);
*/
